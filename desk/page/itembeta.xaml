<Page
    x:Class="desk.page.itembeta"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:desk.page"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <GridView x:Name="Mygridview" >
            <GridView.ItemTemplate >
                <DataTemplate >
                    <Grid Width="150" Height="200" >
                        <Grid  Width="160" Height="250" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image Stretch="UniformToFill" Width="160" Height="250" Source="{Binding itemuri}" Name="Gridimage" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Grid>
                        <StackPanel >
                            
                         <TextBlock Text="{Binding name }" Name="Imagetext"/>
                         <TextBlock Text="{Binding itemuri}" Name="imageurl" />
                        </StackPanel>
                        
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>
